# Snake
Simple snake game written in C# 

#Requirements
 * Dotnet SDK
 * VS 2019

# Usage:
 * Clone the repository
 * Go to project root directory and run
  ```
  dotnet build
  ```
   then
  ```
  dotnet run
  ```
  
